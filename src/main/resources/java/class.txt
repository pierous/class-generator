package ${package}.model.entity;

import javax.persistence.*;

import es.caixagalicia.ifrt.persistencia.eccv02.CEcc;

/**
 * The persistent class for the ${name}C database table.
 */
@Entity
@Table(name = "${table}")
public class ${name}C extends CEcc {

// CONTEXTO

// ATRIBUTOS

${attributes}

}
